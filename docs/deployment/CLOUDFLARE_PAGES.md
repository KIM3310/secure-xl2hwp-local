# Cloudflare Pages Deploy

- Root directory: `.`
- Build command: `(none)`
- Output directory: `site`

Notes:

- This deploy is documentation surface only.
- The secure processing runtime intentionally remains local-first and is not hosted on Pages.
